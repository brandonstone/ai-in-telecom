%YAML 1.1
---
default-rule-path: /etc/suricata/rules
rule-files:
  - local.rules

outputs:
  - fast:
      enabled: yes
      filename: fast.log
  - eve-log:
      enabled: yes
      filename: eve.json

af-packet:
  - interface: eth0
